[project]
name = "FlaskTeamhelper"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "", email = ""},
]
dependencies = ["flask>=3.1.3", "psycopg2>=2.9.12", "dbutils>=3.1.2", "flask-cors>=6.0.2", "pyjwt>=2.13.0"]
requires-python = "==3.11.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
